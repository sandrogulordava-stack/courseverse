{
  "name": "courseverse-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready course marketplace with classrooms, messenger, approvals, moderation and realtime features.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
